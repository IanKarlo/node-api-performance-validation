# Language Model Configuration
# Choose between TypeScript (TS) or Rust (RS) implementations
# LANG_MODEL=TS  # Default: Use TypeScript implementations
# LANG_MODEL=RS  # Use Rust implementations (requires native module)

# Server Configuration
PORT=3000
HOST=localhost

# Node Environment
NODE_ENV=development